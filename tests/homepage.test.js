import { describe, expect, it } from 'vitest'
import fs from 'fs'
import path from 'path'
import { PUBLIC_ROUTES, canonicalUrl, createPageMetadata } from '../lib/site.js'
import { PDF_TOOLS } from '../lib/tools.js'
const root=process.cwd();const pageSource=fs.readFileSync(path.join(root,'app/page.js'),'utf8');const homeSource=fs.readFileSync(path.join(root,'components/tooliyapa/HomePage.js'),'utf8');const layoutSource=fs.readFileSync(path.join(root,'app/layout.js'),'utf8');const toolRoutes=PDF_TOOLS.map(({href})=>href)

describe('homepage content and metadata',()=>{
  it('uses canonical page metadata with one brand mention',()=>{expect(pageSource).toContain('createPageMetadata');expect(pageSource).toContain("pathname: '/'");expect(pageSource).not.toMatch(/noindex|index:\s*false|follow:\s*false/i);const title=pageSource.match(/title:\s*'([^']+)'/)?.[1];const template=layoutSource.match(/template:\s*'([^']+)'/)?.[1];expect(title).toBe('Free Online Tools — PDF, Documents, Text, Calculators & Converters');expect(title).not.toMatch(/tooliyapa/i);expect(template).toBe('%s | Tooliyapa');const renderedTitle=template.replace('%s',title);expect(renderedTitle).toBe('Free Online Tools — PDF, Documents, Text, Calculators & Converters | Tooliyapa');expect(renderedTitle.match(/Tooliyapa/gi)).toHaveLength(1);const metadata=createPageMetadata({title,description:'Homepage',pathname:'/'});expect(metadata.title.absolute).toBe(renderedTitle);expect(metadata.alternates.canonical).toBe(canonicalUrl('/'))})
  it('defines one H1 and server-renderable informational sections',()=>{expect(homeSource.match(/<h1(?:\s|>)/g)).toHaveLength(1);for(const heading of ['What you can do with Tooliyapa','How browser-based processing works','Choose the right PDF tool','Practical tools, clear expectations'])expect(homeSource).toContain(heading)})
  it('keeps all ten public PDF tools registry-backed',()=>{expect(PDF_TOOLS).toHaveLength(10);expect(homeSource).toContain('const TOOLS=PDF_TOOLS.map');for(const route of toolRoutes){expect(PUBLIC_ROUTES).toContain(route);expect(homeSource).toContain(`'${route}': {`)}expect(homeSource).toContain('href={tool.href}')})
  it('activates Documents and Text without overstating behavior',()=>{expect(homeSource).toContain('href="/documents"');expect(homeSource).toContain('href="/text"');expect(homeSource).toContain('{AVAILABLE_TEXT_TOOLS.length} tools available');expect(homeSource).toMatch(/extractive summaries|similarity scores/i)})
  it('describes limited PDF tool behavior accurately',()=>{const presentationBlock=homeSource.match(/const TOOL_PRESENTATION = \{([^]*?)\n\}/)?.[1]??'';expect(presentationBlock).toMatch(/thumbnail-selected pages|one PDF per page/i);expect(presentationBlock).toMatch(/text watermark/i);expect(homeSource).toMatch(/does not guess passwords, crack encryption, or decrypt/i)})
  it('links trust pages and avoids unsupported claims and forbidden hosts',()=>{for(const route of ['/about','/privacy','/contact','/terms','/disclaimer']){expect(PUBLIC_ROUTES).toContain(route);expect(homeSource).toContain(`href="${route}"`)}expect(homeSource).not.toMatch(/Every PDF tool you need|complete suite|no limits|100% private|unlock any PDF/i);expect(`${pageSource}\n${homeSource}`).not.toMatch(/www\.tooliyapa\.com|http:\/\/tooliyapa\.com|localhost|staging|Emergent/i)})
})
