import { TextToolPage } from '@/components/tooliyapa/TextUI'
import { textMetadata } from '@/lib/text-tools'
export const metadata = textMetadata('grammar-writing')
export default function Page(){ return <TextToolPage toolId="grammar-writing"/> }
